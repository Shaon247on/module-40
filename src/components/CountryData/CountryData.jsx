const CountryData = ({country, handleVisitedCountry, handlsVisitedFlags}) => {
    return (
        <div>
            <h5>Country Data:{country.name.common}</h5>

        </div>
    );
};

export default CountryData;